# Interop

OPL may send `load_run_id` correlation to OPA hub when `PEER_OPA_URL` is set. Empty peer URLs disable correlation features.

Service JWTs use `OPEN_SERVICE_JWT_SECRET` with `iss`/`aud`/`scope`.
